# Ruleset Enforcement Smoke Test

Temporary, documentation-only file used to verify that the TAROT BREAKER `main` ruleset requires the configured GitHub Actions Main Merge Gate before merge.

This file changes no production runtime, gameplay, assets, routes, audio, events, save state, or UI.

Expected result: the pull request is evaluated by `Event Safety / Main Merge Gate` before it can merge into `main`.
