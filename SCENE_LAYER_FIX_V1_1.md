# 星門庭園 動的2.5D・レイヤー修正 v1.1

2026-09-14。基点は `feature/lumiere-gate-npc` の `9e33c56`（PUBLIC PREVIEW 29）。

## 変更

- A: 背景・前景の旧門を実行時マスクで除外し、既存の星空素材で背面を補完。新しい門の開口部の中心を階段中央 x=800 に合わせた。門本体は1オブジェクト。リュミエールは (810,212) を維持。
- B: Foregroundを静的な背面として描画し、柱・花壇前縁・門・噴水前縁の17領域だけを条件付き遮蔽として扱う。`actorFootY < baseline` と足元エリアの両条件で判定し、各Actorを別の小さなCanvasでマスクする。手前の別Actorを巻き込まない。長い橋脚や遠景建築は遮蔽しない。
- B: 独立したSolid Baseを既存のblockedAreas処理へ合成し、噴水と一部の柱の根元を通れなくした。手描きJSON・Collision Editorは変更しない。ユーザーのALLOW/BLOCKEDに将来OCCLUSIONを追加する場合は、`scene-layout.js` の `id / bounds / points / baseline / footArea / source` を使える。
- C: 雲は遠景32px/80秒・近景64px/48秒の往復。滝は16px/2.2秒で落下し、半周期ずれの2枚と固定マスクでループの戻りを隠す。
- C: 水晶の浮遊12px/4.4秒、金環24秒、Glow 3.1秒、水面4.7秒、Sparkle 2.6秒。金環と水晶が一体の原画は、青い面を既存素材から補い、金環を元の形に合わせたCanvas描画として分離。水晶の姿勢を維持したまま金環が回る。
- C: 門の内光0.45–0.70/5秒、粒子0.12–0.22/7.6秒。Event FXは通常・unstableで非表示、event時だけ表示。
- Reduced Motion: 雲は半分未満、金環/水晶浮遊/水面変形を停止、滝は5秒へ減速、粒子減少、穏やかなGlowは継続。
- 描画同期: 別RAFと毎フレームのgetBoundingClientRectを廃止し、ゲームと同じカメラ値で同一フレームに同期。読み込み完了前に探索を開始しない。
- 公開同期: 全ランタイムを同じ版で揃え、画像/BGMをコミット固定。公開側の当たり判定JSONとEditorを維持。

## 検証

- 構文、素材、音声設定の検証と52件の自動テスト: PASS。
- 足元エリア・baseline境界・複数Actorの遮蔽分離・縮尺補正・小規模Solid Base追加後の経路接続を検証。
- 実素材のCanvas描画で旧門除去、位置合わせ、金環の複数回転角を確認。
- 公開ブラウザ・GitHub Actions: 更新後に確認。
- iPhone/iPad実機: 未確認。実機完了とは扱わない。

## 確認方法

通常探索: 開始地点 → 左右の橋 → 花壇 → 噴水 → 上側通路 → 星門階段 → リュミエール → 門。

`?sceneDebug=1` で、噴水は水色、門は黄色、遮蔽は青、Actor足元は白。青い点線はbaseline、青い塗りは足元の有効エリア。デバッグ下部にはActor別の有効遮蔽IDが出る。

`?navDebug=1&sceneDebug=1&viewport=390x844` はスマホ相当の確認用。`?gateState=event` はイベントFX、通常はクエリを付けない。

## 残る実機判定

小さな画面で雲・滝・浮遊・金環回転が認識できるか、柱や花壇の裏へ入る位置が自然か、長時間操作・Safariの描画負荷・音声を確認する。ゲーム本体mainへの統合は既存方針どおり実機確認後。
